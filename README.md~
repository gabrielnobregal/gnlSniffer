1)How to compile:

gcc gnlSniffer.c -o sniffer

2)How to execute:

ifconfig eth0 promisc
./sniffer -i eth0

OBS: eth0 is the active network interface

3)Sniffer options and execution modes are defined in help:

./sniffer -help